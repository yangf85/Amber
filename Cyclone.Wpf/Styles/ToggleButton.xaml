<!--
    ============================================================================
    ToggleButton 样式 (跟项目其他控件视觉一致)
    
    设计:
    - Unchecked: 白底 + 灰边框 + 灰字 (跟 Button 默认态一致)
    - Checked: 主色实心 + 主色边框 + 白字 (跟 CheckBox/Pagination 选中态一致)
    - Hover:
    - Unchecked: 边框变主色淡边
    - Checked: 背景颜色稍微变深 (Background.Highlighted)
    - Pressed: 整体 Opacity 0.85
    - IsKeyboardFocused: 边框变 Border.Focused (Button 风格一致)
    - Disabled: 整体 Opacity 0.5
    
    改动 (vs 旧版):
    - 删 AccentToggleButton — 引用全是不存在的 token (AccentBrush/CheckedBackgroundBrush/...)
    实际 fallback 到 null,从未真正生效
    - Setter Property 显式 TargetName="RootBorder" (旧版有些 Setter 不写 TargetName,改的是控件 DP)
    - 加 IsKeyboardFocused trigger
    - Hover-Checked 多触发器:背景变 Background.Highlighted (旧版变 Hover 灰色,Checked 态视觉退化)
    - Height 写死 → MinHeight + 加 Padding (适应多行文字)
    ============================================================================
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Styles/Control.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style
        x:Key="ToggleButton.Style.Basic"
        BasedOn="{StaticResource Control.Style.Basic}"
        TargetType="{x:Type ToggleButton}">
        <Setter Property="MinHeight" Value="{DynamicResource Height.Horizontal.Control}" />
        <Setter Property="Padding" Value="14,4" />
        <Setter Property="Background" Value="{DynamicResource Background.Container}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            x:Name="ContentContainer"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  Hover (unchecked)  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Hover}" />
                        </MultiTrigger>

                        <!--  Checked — 主色实心 + 白字  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Selected}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Background.Selected}" />
                            <Setter Property="Foreground" Value="{DynamicResource Foreground.Invert}" />
                        </Trigger>

                        <!--  Hover-Checked — 背景稍变深  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Highlighted}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Background.Highlighted}" />
                        </MultiTrigger>

                        <!--  Pressed — Opacity 略降  -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Opacity" Value="0.85" />
                        </Trigger>

                        <!--  IsKeyboardFocused — 边框变 Focused  -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Focused}" />
                        </Trigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource ToggleButton.Style.Basic}" TargetType="{x:Type ToggleButton}" />
</ResourceDictionary>
