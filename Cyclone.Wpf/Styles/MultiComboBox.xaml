<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctl="clr-namespace:Cyclone.Wpf.Controls">

    <!--
        =================================================================
        ToggleButton（下拉箭头）
        =================================================================
    -->

    <Style x:Key="MultiComboBox.ToggleButton.Style.Basic" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid Background="{TemplateBinding Background}">
                        <Path
                            x:Name="Arrow"
                            Width="15"
                            Height="15"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Data="M533.333333 631.466667L866.133333 298.666667l29.866667 29.866666-362.666667 362.666667L170.666667 328.533333l29.866666-29.866666 332.8 332.8z"
                            Fill="{DynamicResource Icon.Foreground.Default}"
                            Stretch="Uniform" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource Icon.Foreground.Hover}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Arrow" Property="Data" Value="M533.333333 358.4l-332.8 332.8-29.866666-29.866667L533.333333 298.666667l362.666667 362.666666-29.866667 29.866667-332.8-332.8z" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        =================================================================
        ClearButton（清除选中项）
        =================================================================
    -->

    <Style x:Key="MultiComboBox.ClearButton.Style.Basic" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="Transparent">
                        <Path
                            x:Name="Icon"
                            Width="12"
                            Height="12"
                            Data="M558.933333 529.066667l285.866667 285.866666-29.866667 29.866667-285.866666-285.866667-285.866667 285.866667-29.866667-29.866667 285.866667-285.866666L213.333333 243.2l29.866667-29.866667 285.866667 285.866667L814.933333 213.333333l29.866667 29.866667-285.866667 285.866667z"
                            Fill="{DynamicResource Icon.Foreground.Default}"
                            Stretch="Uniform" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Icon" Property="Fill" Value="{DynamicResource Icon.Foreground.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Icon" Property="Fill" Value="{DynamicResource Icon.Foreground.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Icon" Property="Fill" Value="{DynamicResource Icon.Foreground.Disabled}" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        =================================================================
        默认 Chip 模板：边框 + 文本 + 删除按钮
        DataContext = 数据项本身
        通过 ContentPresenter + DisplayMemberPath（在外层 ItemsControl 设）显示文字
        =================================================================
    -->

    <Style x:Key="MultiComboBox.ChipDeleteButton.Style.Basic" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}">
                        <Path
                            x:Name="Cross"
                            Width="8"
                            Height="8"
                            Data="M558.933333 529.066667l285.866667 285.866666-29.866667 29.866667-285.866666-285.866667-285.866667 285.866667-29.866667-29.866667 285.866667-285.866666L213.333333 243.2l29.866667-29.866667 285.866667 285.866667L814.933333 213.333333l29.866667 29.866667-285.866667 285.866667z"
                            Fill="{DynamicResource Icon.Foreground.Default}"
                            Stretch="Uniform" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource Background.Hover}" />
                            <Setter TargetName="Cross" Property="Fill" Value="{DynamicResource Foreground.Default}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        默认 chip 模板由 MultiComboBox.cs 在 OnApplyTemplate / DisplayMemberPath 变化时动态生成
        因为 DataTemplate 在 xaml 里是静态的，不能根据外层控件的 DisplayMemberPath 动态构造 Binding 路径。
        ChipDeleteButton.Style.Basic 仍然作为静态资源供动态生成的模板使用。
    -->

    <!--
        =================================================================
        MultiComboBoxItem（下拉里每个 item 的视觉）
        =================================================================
    -->

    <ControlTemplate x:Key="MultiComboBoxItem.ControlTemplate.Basic" TargetType="{x:Type ctl:MultiComboBoxItem}">
        <Border
            x:Name="RootBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <CheckBox
                    Grid.Column="0"
                    Margin="6,0,2,0"
                    VerticalAlignment="Center"
                    Focusable="False"
                    IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                    IsHitTestVisible="False" />

                <ContentPresenter
                    Grid.Column="1"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Content="{TemplateBinding Content}"
                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Selected}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="MultiComboBoxItem.Style.Basic" TargetType="{x:Type ctl:MultiComboBoxItem}">
        <Setter Property="Template" Value="{StaticResource MultiComboBoxItem.ControlTemplate.Basic}" />
        <Setter Property="Height" Value="{DynamicResource Height.Horizontal.Item}" />
        <Setter Property="Padding" Value="{DynamicResource Padding.Horizontal}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style BasedOn="{StaticResource MultiComboBoxItem.Style.Basic}" TargetType="{x:Type ctl:MultiComboBoxItem}" />

    <!--
        =================================================================
        MultiComboBox（主控件）
        =================================================================
    -->

    <ControlTemplate x:Key="MultiComboBox.ControlTemplate.Basic" TargetType="{x:Type ctl:MultiComboBox}">
        <Border
            x:Name="PART_RootBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--
                    显示区域：
                    - chip 模式：ItemsControl 渲染所有选中项（SelectedItemsBindable）
                    - 没选中项时显示 Watermark
                    点击空白区域打开下拉（通过 LeftButtonDown 路由到 ToggleButton 也可，但这里靠 Popup.StaysOpen=False + ToggleButton 已经够）
                -->
                <Grid Grid.Column="0" Margin="2">
                    <ItemsControl
                        x:Name="PART_ChipPanel"
                        IsTabStop="False"
                        ItemTemplate="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=SelectionChipTemplate, FallbackValue={x:Null}}"
                        ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=SelectedItemsBindable}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <TextBlock
                        x:Name="WatermarkBlock"
                        Margin="6,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource Text.Prompt}"
                        IsHitTestVisible="False"
                        Text="{TemplateBinding Watermark}"
                        TextTrimming="CharacterEllipsis"
                        Visibility="Collapsed" />
                </Grid>

                <!--  清除按钮  -->
                <Button
                    x:Name="PART_ClearButton"
                    Grid.Column="1"
                    Margin="5,0"
                    VerticalAlignment="Center"
                    Command="{x:Static ctl:MultiComboBox.ClearCommand}"
                    Style="{StaticResource MultiComboBox.ClearButton.Style.Basic}" />

                <!--  下拉切换按钮  -->
                <ToggleButton
                    x:Name="PART_ToggleButton"
                    Grid.Column="2"
                    Margin="0,0,5,0"
                    VerticalAlignment="Center"
                    IsChecked="{Binding IsOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                    Style="{StaticResource MultiComboBox.ToggleButton.Style.Basic}" />

                <!--  弹出层  -->
                <Popup
                    x:Name="PART_Popup"
                    AllowsTransparency="True"
                    IsOpen="{Binding IsOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                    Placement="Bottom"
                    PlacementTarget="{Binding ElementName=PART_RootBorder}"
                    PopupAnimation="Slide"
                    StaysOpen="True"
                    VerticalOffset="1">
                    <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                        <Border
                            x:Name="ContainerBorder"
                            Background="{DynamicResource Background.Container}"
                            BorderBrush="{DynamicResource Border.Default}"
                            BorderThickness="1">
                            <DockPanel>
                                <!--  全选 CheckBox（顶部）  -->
                                <StackPanel x:Name="SelectAllPanel" DockPanel.Dock="Top">
                                    <CheckBox
                                        x:Name="PART_SelectAllCheckBox"
                                        Margin="6,4,6,2"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding SelectAllText}"
                                        FontWeight="Bold" />
                                    <Rectangle
                                        Height="0.5"
                                        Margin="2,2,2,0"
                                        Fill="{Binding ElementName=ContainerBorder, Path=BorderBrush}" />
                                </StackPanel>

                                <!--  Items（基类滚动条 + ItemsPresenter）  -->
                                <ScrollViewer
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Hover}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="PART_RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Focused}" />
            </Trigger>
            <Trigger Property="IsOpen" Value="True">
                <Setter TargetName="PART_RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Active}" />
            </Trigger>

            <!--  没有选中项：显示水印、隐藏清除按钮  -->
            <Trigger Property="HasSelectedItems" Value="False">
                <Setter TargetName="WatermarkBlock" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
            </Trigger>

            <!--  全选 CheckBox 不显示  -->
            <Trigger Property="IsSelectAllVisible" Value="False">
                <Setter TargetName="SelectAllPanel" Property="Visibility" Value="Collapsed" />
            </Trigger>

            <!--  用户禁用了清除按钮：始终隐藏  -->
            <Trigger Property="IsClearButtonVisible" Value="False">
                <Setter TargetName="PART_ClearButton" Property="Visibility" Value="Collapsed" />
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
                <Setter TargetName="PART_RootBorder" Property="Background" Value="{DynamicResource Background.Disabled}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="MultiComboBox.Style.Basic" TargetType="{x:Type ctl:MultiComboBox}">
        <Setter Property="Template" Value="{StaticResource MultiComboBox.ControlTemplate.Basic}" />
        <Setter Property="Background" Value="{DynamicResource Background.Default}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="MinHeight" Value="{DynamicResource Height.Horizontal.Control}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource MultiComboBox.Style.Basic}" TargetType="{x:Type ctl:MultiComboBox}" />
</ResourceDictionary>