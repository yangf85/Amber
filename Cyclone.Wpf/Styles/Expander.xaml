<!--
    ============================================================================
    Expander 样式 (WinUI 3 风格 · 无圆角 · LayoutTransform 真实布局动画)
    
    设计要点:
    - Header 用 ToggleButton 模板——完整 hover / pressed / focused / disabled 视觉
    - 箭头用 RotateTransform 平滑旋转 180° (替代多组 Path Data 切换)
    - 内容展开/折叠用 LayoutTransform.ScaleY
      · 与 RenderTransform 不同——ScaleY 真实参与布局测量,
        内容 layout slot 高度从 0 平滑增长到全高,
        外部布局的其他控件会被平滑推开/收回,不会瞬间跳位
      · 无需手动测量目标高度——WPF 自己算,
        内容可以动态变化高度,无需缓存 / 锁死
    - 无圆角 (与项目规约对齐)
    - Down 方向:Content 在 Header 下方,边框分隔线在 Content 顶部
      Up 方向:Header 在 Content 下方,边框分隔线移到 Content 底部
    
    注:伴随这份样式,Cyclone.Wpf.Helpers.ExpanderHelper 可以删除——
       样式已包含完整的动画,无需附加属性介入。
    ============================================================================
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Styles/Control.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  ============================================================  -->
    <!--  Expander Header 的 ToggleButton 样式  -->
    <!--  ============================================================  -->
    <Style x:Key="Expander.HeaderToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        SnapsToDevicePixels="True">
                        <Grid Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Header content  -->
                            <ContentPresenter
                                x:Name="ContentSite"
                                Grid.Column="0"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />

                            <!--  展开/折叠箭头——RotateTransform 平滑旋转 180°  -->
                            <Path
                                x:Name="Arrow"
                                Grid.Column="1"
                                Width="12"
                                Height="12"
                                Margin="8,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="M512 606.29333299L790.698667 341.333333 853.333333 400.853333 512 725.333333l-341.333333-324.48L233.301333 341.333333z"
                                Fill="{DynamicResource Icon.Foreground.Default}"
                                RenderTransformOrigin="0.5,0.5"
                                Stretch="Uniform"
                                StrokeThickness="0">
                                <Path.RenderTransform>
                                    <RotateTransform x:Name="ArrowRotate" Angle="0" />
                                </Path.RenderTransform>
                            </Path>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Checked——箭头旋转 180° (向上方向时由外层 Expander trigger 重设起始角)  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ArrowRotate"
                                            Storyboard.TargetProperty="Angle"
                                            To="180"
                                            Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ArrowRotate"
                                            Storyboard.TargetProperty="Angle"
                                            To="0"
                                            Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!--  Hover  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource Icon.Foreground.Hover}" />
                        </Trigger>

                        <!--  Pressed  -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Pressed}" />
                        </Trigger>

                        <!--  Focused (键盘 Tab 进入)  -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
                        </Trigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource Icon.Foreground.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ============================================================  -->
    <!--  Expander 样式  -->
    <!--  ============================================================  -->
    <Style
        x:Key="Expander.Style.Basic"
        BasedOn="{StaticResource Control.Style.Basic}"
        TargetType="Expander">
        <Setter Property="Background" Value="{DynamicResource Background.Container}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  Header  -->
                            <ToggleButton
                                x:Name="HeaderContainer"
                                Grid.Row="0"
                                MinHeight="{DynamicResource Height.Horizontal.Control}"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                Style="{StaticResource Expander.HeaderToggleButtonStyle}" />

                            <!--
                                内容区——LayoutTransform.ScaleY 让缩放真实参与布局测量。
                                ScaleY=0 时 layout slot 高度真为 0,
                                ScaleY 渐变到 1 时 slot 高度平滑增长——外部布局的其他控件被平滑推开。
                                
                                初始状态:Visibility=Collapsed (彻底从布局退出) + ScaleY=0,
                                双重保险,避免首次渲染闪一下。
                            -->
                            <Border
                                x:Name="ContentBorder"
                                Grid.Row="1"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,1,0,0"
                                Visibility="Collapsed">
                                <Border.LayoutTransform>
                                    <ScaleTransform x:Name="ContentScale" ScaleY="0" />
                                </Border.LayoutTransform>
                                <ContentPresenter
                                    x:Name="ContentContainer"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Up 方向——Header 和 Content 交换 Row,分隔线从顶部挪到底部  -->
                        <Trigger Property="ExpandDirection" Value="Up">
                            <Setter TargetName="ContentBorder" Property="Grid.Row" Value="0" />
                            <Setter TargetName="HeaderContainer" Property="Grid.Row" Value="1" />
                            <Setter TargetName="ContentBorder" Property="BorderThickness" Value="0,0,0,1" />
                        </Trigger>

                        <!--  展开 / 折叠动画  -->
                        <Trigger Property="IsExpanded" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!--  先打开 Visibility,LayoutTransform 才能开始工作  -->
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <!--  ScaleY 0→1:layout slot 高度从 0 平滑增长到全高  -->
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentScale"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="1"
                                            Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <!--  伴随 Opacity 淡入——避免被压扁的瞬间过于显眼  -->
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentBorder"
                                            Storyboard.TargetProperty="Opacity"
                                            From="0"
                                            To="1"
                                            Duration="0:0:0.22" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentScale"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="0"
                                            Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentBorder"
                                            Storyboard.TargetProperty="Opacity"
                                            From="1"
                                            To="0"
                                            Duration="0:0:0.15" />
                                        <!--  ScaleY 到 0 之后再 Collapse,释放 hit-test  -->
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentBorder" Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.18" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!--  Disabled——整体淡化  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  默认样式  -->
    <Style BasedOn="{StaticResource Expander.Style.Basic}" TargetType="Expander" />
</ResourceDictionary>
