<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctl="clr-namespace:Cyclone.Wpf.Controls">

    <!--
        =================================================================
        Thumb 样式
        Width / Height 不在此设置——由模板通过 TemplateBinding ThumbSize 提供
        =================================================================
    -->

    <Style x:Key="RangeSlider.Thumb.Style.Basic" TargetType="{x:Type Thumb}">
        <Setter Property="Background" Value="{DynamicResource Background.Default}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Hover}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Border.Focused}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Pressed}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        =================================================================
        RepeatButton 样式(横向轨道段 / 纵向轨道段)
        =================================================================
    -->

    <Style x:Key="RangeSlider.RepeatButton.Style.Horizontal" TargetType="{x:Type RepeatButton}">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RangeSlider.RepeatButton.Style.Vertical" TargetType="{x:Type RepeatButton}">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        =================================================================
        水平模板
        =================================================================
    -->

    <ControlTemplate x:Key="RangeSlider.ControlTemplate.Horizontal" TargetType="{x:Type ctl:RangeSlider}">
        <Grid SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  顶部刻度线  -->
            <TickBar
                x:Name="TopTickBar"
                Grid.Row="0"
                Height="4"
                Fill="{TemplateBinding Foreground}"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Placement="Top"
                ReservedSpace="{TemplateBinding ThumbSize}"
                TickFrequency="{TemplateBinding TickFrequency}"
                Ticks="{TemplateBinding Ticks}"
                Visibility="Collapsed" />

            <!--  Track —— 双 thumb 动态布局  -->
            <ctl:RangeTrack
                Grid.Row="1"
                VerticalAlignment="Center"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                LowerValue="{TemplateBinding LowerValue}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Orientation="{TemplateBinding Orientation}"
                UpperValue="{TemplateBinding UpperValue}">
                <ctl:RangeTrack.StartRegion>
                    <RepeatButton
                        x:Name="PART_StartRegion"
                        Height="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding InactiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Horizontal}" />
                </ctl:RangeTrack.StartRegion>
                <ctl:RangeTrack.StartThumb>
                    <Thumb
                        x:Name="PART_StartThumb"
                        Width="{TemplateBinding ThumbSize}"
                        Height="{TemplateBinding ThumbSize}"
                        Style="{StaticResource RangeSlider.Thumb.Style.Basic}" />
                </ctl:RangeTrack.StartThumb>
                <ctl:RangeTrack.MiddleRegion>
                    <RepeatButton
                        x:Name="PART_MiddleRegion"
                        Height="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding ActiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Horizontal}" />
                </ctl:RangeTrack.MiddleRegion>
                <ctl:RangeTrack.EndThumb>
                    <Thumb
                        x:Name="PART_EndThumb"
                        Width="{TemplateBinding ThumbSize}"
                        Height="{TemplateBinding ThumbSize}"
                        Style="{StaticResource RangeSlider.Thumb.Style.Basic}" />
                </ctl:RangeTrack.EndThumb>
                <ctl:RangeTrack.EndRegion>
                    <RepeatButton
                        x:Name="PART_EndRegion"
                        Height="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding InactiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Horizontal}" />
                </ctl:RangeTrack.EndRegion>
            </ctl:RangeTrack>

            <!--  底部刻度线  -->
            <TickBar
                x:Name="BottomTickBar"
                Grid.Row="2"
                Height="4"
                Fill="{TemplateBinding Foreground}"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Placement="Bottom"
                ReservedSpace="{TemplateBinding ThumbSize}"
                TickFrequency="{TemplateBinding TickFrequency}"
                Ticks="{TemplateBinding Ticks}"
                Visibility="Collapsed" />
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="TopTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="BottomTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="TopTickBar" Property="Visibility" Value="Visible" />
                <Setter TargetName="BottomTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--
        =================================================================
        垂直模板
        垂直方向:底部 = Minimum,顶部 = Maximum
        =================================================================
    -->

    <ControlTemplate x:Key="RangeSlider.ControlTemplate.Vertical" TargetType="{x:Type ctl:RangeSlider}">
        <Grid SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  左侧刻度线  -->
            <TickBar
                x:Name="LeftTickBar"
                Grid.Column="0"
                Width="4"
                Fill="{TemplateBinding Foreground}"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Placement="Left"
                ReservedSpace="{TemplateBinding ThumbSize}"
                TickFrequency="{TemplateBinding TickFrequency}"
                Ticks="{TemplateBinding Ticks}"
                Visibility="Collapsed" />

            <!--  Track —— 双 thumb 动态布局  -->
            <ctl:RangeTrack
                Grid.Column="1"
                HorizontalAlignment="Center"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                LowerValue="{TemplateBinding LowerValue}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Orientation="{TemplateBinding Orientation}"
                UpperValue="{TemplateBinding UpperValue}">
                <ctl:RangeTrack.StartRegion>
                    <RepeatButton
                        x:Name="PART_StartRegion"
                        Width="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding InactiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Vertical}" />
                </ctl:RangeTrack.StartRegion>
                <ctl:RangeTrack.StartThumb>
                    <Thumb
                        x:Name="PART_StartThumb"
                        Width="{TemplateBinding ThumbSize}"
                        Height="{TemplateBinding ThumbSize}"
                        Style="{StaticResource RangeSlider.Thumb.Style.Basic}" />
                </ctl:RangeTrack.StartThumb>
                <ctl:RangeTrack.MiddleRegion>
                    <RepeatButton
                        x:Name="PART_MiddleRegion"
                        Width="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding ActiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Vertical}" />
                </ctl:RangeTrack.MiddleRegion>
                <ctl:RangeTrack.EndThumb>
                    <Thumb
                        x:Name="PART_EndThumb"
                        Width="{TemplateBinding ThumbSize}"
                        Height="{TemplateBinding ThumbSize}"
                        Style="{StaticResource RangeSlider.Thumb.Style.Basic}" />
                </ctl:RangeTrack.EndThumb>
                <ctl:RangeTrack.EndRegion>
                    <RepeatButton
                        x:Name="PART_EndRegion"
                        Width="{TemplateBinding TrackThickness}"
                        Background="{TemplateBinding InactiveTrackBrush}"
                        Style="{StaticResource RangeSlider.RepeatButton.Style.Vertical}" />
                </ctl:RangeTrack.EndRegion>
            </ctl:RangeTrack>

            <!--  右侧刻度线  -->
            <TickBar
                x:Name="RightTickBar"
                Grid.Column="2"
                Width="4"
                Fill="{TemplateBinding Foreground}"
                IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                Maximum="{TemplateBinding Maximum}"
                Minimum="{TemplateBinding Minimum}"
                Placement="Right"
                ReservedSpace="{TemplateBinding ThumbSize}"
                TickFrequency="{TemplateBinding TickFrequency}"
                Ticks="{TemplateBinding Ticks}"
                Visibility="Collapsed" />
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
                <Setter TargetName="LeftTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
                <Setter TargetName="RightTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="TickPlacement" Value="Both">
                <Setter TargetName="LeftTickBar" Property="Visibility" Value="Visible" />
                <Setter TargetName="RightTickBar" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--
        =================================================================
        基础样式
        =================================================================
    -->

    <Style x:Key="RangeSlider.Style.Basic" TargetType="{x:Type ctl:RangeSlider}">
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="InactiveTrackBrush" Value="{DynamicResource Background.Inactive}" />
        <Setter Property="ActiveTrackBrush" Value="{DynamicResource Background.Highlighted}" />
        <Setter Property="TrackThickness" Value="4" />
        <Setter Property="ThumbSize" Value="16" />
        <Setter Property="TickFrequency" Value="1" />
        <Setter Property="AutoToolTipPrecision" Value="1" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource RangeSlider.ControlTemplate.Horizontal}" />
                <Setter Property="MinHeight" Value="20" />
                <Setter Property="MinWidth" Value="100" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource RangeSlider.ControlTemplate.Vertical}" />
                <Setter Property="MinWidth" Value="20" />
                <Setter Property="MinHeight" Value="100" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource RangeSlider.Style.Basic}" TargetType="{x:Type ctl:RangeSlider}" />
</ResourceDictionary>