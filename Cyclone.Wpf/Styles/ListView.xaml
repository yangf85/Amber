<!--
    ============================================================================
    WinUI 3 风格 ListView 样式 (v4 — 修复 GridView 列绑定失效)

    破坏性变更说明（vs v3）：
    - 修复 ListViewItem 模板使 GridView.Columns 真正生效：
    · v3 错误地认为 ContentPresenter 在 View=GridView 时会被 WPF
    自动替换为 GridViewRowPresenter——这个说法不成立
    · 实际机制：默认 ListViewItem 模板必须用 GridViewRowPresenter，
    框架在 GridView.PrepareItem 时把列内容注入这个 presenter
    · v4 改为：默认模板用 GridViewRowPresenter（GridView 必需），
    通过 Style.Triggers 检测 ListView.View 为 null 时切到 ContentPresenter
    模板——既支持 GridView 列模式，也支持脱了 View 的 ItemTemplate 模式
    - 修复 GridViewHeaderRowPresenter 的 ColumnHeaderContainerStyle 链路
    （v3 已修复 Visibility 联动 View，本次未变）

    破坏性变更说明（vs 原始 Win10 版本）：
    - WinUI 3 视觉风格：左侧 3px 竖条选中指示器（pill, 仅选中态淡入）
    - 整行 hover/选中浅底，文字色保持不变（不再反色）
    - 失焦选中：指示器弱化为 Highlighted, 行底透明
    - 无圆角
    - GridViewColumnHeader 单独样式化（hover/按下交互 + 拖动调宽）
    ============================================================================
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Styles/Control.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  ============================================================  -->
    <!--  GridViewColumnHeader 样式 (列头, 仅 GridView 模式用)  -->
    <!--  ============================================================  -->
    <Style x:Key="GridViewColumnHeader.Style.Default" TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Background" Value="{DynamicResource Background.Header}" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Caption}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                    <Grid>
                        <Border
                            x:Name="HeaderBorder"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True" />
                        </Border>
                        <Thumb
                            x:Name="PART_HeaderGripper"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            Cursor="SizeWE">
                            <Thumb.Style>
                                <Style TargetType="{x:Type Thumb}">
                                    <Setter Property="Width" Value="6" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Thumb.Style>
                        </Thumb>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
                            <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource Background.Highlighted}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="Role" Value="Padding">
                            <Setter TargetName="HeaderBorder" Property="BorderThickness" Value="0,0,0,1" />
                            <Setter TargetName="PART_HeaderGripper" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource GridViewColumnHeader.Style.Default}" TargetType="{x:Type GridViewColumnHeader}" />

    <!--  ============================================================  -->
    <!--  ListViewItem 模板片段：定义两套，靠 trigger 切换  -->
    <!--  ============================================================  -->

    <!--  GridView 模式模板：用 GridViewRowPresenter 渲染列  -->
    <ControlTemplate x:Key="ListViewItem.Template.GridView" TargetType="{x:Type ListViewItem}">
        <Border
            x:Name="RootBorder"
            Padding="{TemplateBinding Padding}"
            HorizontalAlignment="Stretch"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="SelectionBar"
                    Grid.Column="0"
                    Width="3"
                    Height="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{DynamicResource Background.Selected}"
                    Opacity="0">
                    <Border.RenderTransform>
                        <ScaleTransform x:Name="SelectionBarScale" ScaleY="0.4" />
                    </Border.RenderTransform>
                    <Border.RenderTransformOrigin>
                        <Point X="0.5" Y="0.5" />
                    </Border.RenderTransformOrigin>
                </Border>

                <!--  GridView 列内容 — 框架通过 GridView.PrepareItem 注入  -->
                <GridViewRowPresenter
                    Grid.Column="1"
                    Margin="4,0,8,0"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Columns="{Binding View.Columns, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}"
                    Content="{TemplateBinding Content}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </MultiTrigger>

            <Trigger Property="IsSelected" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBar"
                                Storyboard.TargetProperty="Opacity"
                                To="1"
                                Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBarScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="1"
                                Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <BackEase Amplitude="0.3" EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBar"
                                Storyboard.TargetProperty="Opacity"
                                To="0"
                                Duration="0:0:0.10" />
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBarScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="0.4"
                                Duration="0:0:0.10" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </Trigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="Selector.IsSelectionActive" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBar" Property="Background" Value="{DynamicResource Background.Highlighted}" />
                <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
            </MultiTrigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
                <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  普通列表模式模板：用 ContentPresenter 渲染 ItemTemplate  -->
    <ControlTemplate x:Key="ListViewItem.Template.Plain" TargetType="{x:Type ListViewItem}">
        <Border
            x:Name="RootBorder"
            Padding="{TemplateBinding Padding}"
            HorizontalAlignment="Stretch"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="SelectionBar"
                    Grid.Column="0"
                    Width="3"
                    Height="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{DynamicResource Background.Selected}"
                    Opacity="0">
                    <Border.RenderTransform>
                        <ScaleTransform x:Name="SelectionBarScale" ScaleY="0.4" />
                    </Border.RenderTransform>
                    <Border.RenderTransformOrigin>
                        <Point X="0.5" Y="0.5" />
                    </Border.RenderTransformOrigin>
                </Border>

                <ContentPresenter
                    Grid.Column="1"
                    Margin="4,0,8,0"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Content="{TemplateBinding Content}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </MultiTrigger>

            <Trigger Property="IsSelected" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBar"
                                Storyboard.TargetProperty="Opacity"
                                To="1"
                                Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBarScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="1"
                                Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <BackEase Amplitude="0.3" EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBar"
                                Storyboard.TargetProperty="Opacity"
                                To="0"
                                Duration="0:0:0.10" />
                            <DoubleAnimation
                                Storyboard.TargetName="SelectionBarScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="0.4"
                                Duration="0:0:0.10" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Background.Hover}" />
            </Trigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True" />
                    <Condition Property="Selector.IsSelectionActive" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="SelectionBar" Property="Background" Value="{DynamicResource Background.Highlighted}" />
                <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
            </MultiTrigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Disabled}" />
                <Setter TargetName="RootBorder" Property="Background" Value="Transparent" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  ============================================================  -->
    <!--  ListViewItem 样式: 默认用 GridView 模板, View=null 时切普通模板  -->
    <!--  ============================================================  -->
    <Style x:Key="ListViewItem.Style.Default" TargetType="{x:Type ListViewItem}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="{DynamicResource Height.Horizontal.Item}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <!--  默认走 GridView 模板（绝大多数 ListView 用法）  -->
        <Setter Property="Template" Value="{StaticResource ListViewItem.Template.GridView}" />
        <Style.Triggers>
            <!--  隔行换色：AlternationCount 必须 >= 2，奇数行使用替代背景  -->
            <DataTrigger Binding="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource Self}}" Value="1">
                <Setter Property="Background" Value="{DynamicResource Background.AlternateRow}" />
            </DataTrigger>

            <!--
                ListView.View=null 时切普通模板——通过 RelativeSource 找父 ListView.View 判定
                关键：DataTrigger 比 Setter 优先级高,在父级 View 为 null 时覆盖默认 Template
            -->
            <DataTrigger Binding="{Binding View, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListView}}}" Value="{x:Null}">
                <Setter Property="Template" Value="{StaticResource ListViewItem.Template.Plain}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource ListViewItem.Style.Default}" TargetType="{x:Type ListViewItem}" />

    <!--  ============================================================  -->
    <!--  ListView 样式  -->
    <!--  ============================================================  -->
    <Style
        x:Key="ListView.Style.Default"
        BasedOn="{StaticResource Control.Style.Basic}"
        TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="{DynamicResource Background.Default}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="AlternationCount" Value="2" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ScrollViewer
                            Padding="{TemplateBinding Padding}"
                            CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                            Focusable="False"
                            HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                            VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <!--
                                    与 ListViewItem.Template.GridView 中的行内容区保持同样结构：
                                    左侧 6px 选中条占位 + 内容区 Margin=4,0,8,0。
                                    否则表头从 x=0 开始，行内容从 x=10 开始，列会错位。
                                -->
                                <Grid x:Name="HeaderHost" Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="6" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <GridViewHeaderRowPresenter
                                        x:Name="HeaderRow"
                                        Grid.Column="1"
                                        Margin="4,0,8,0"
                                        Columns="{Binding View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                        <GridViewHeaderRowPresenter.Style>
                                            <Style TargetType="{x:Type GridViewHeaderRowPresenter}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding View, RelativeSource={RelativeSource AncestorType=ListView}}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </GridViewHeaderRowPresenter.Style>
                                    </GridViewHeaderRowPresenter>
                                </Grid>

                                <ItemsPresenter Grid.Row="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="True" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource ListView.Style.Default}" TargetType="{x:Type ListView}" />
</ResourceDictionary>